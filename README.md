# Monitoring Splits (Streamlit)

## Setup (Windows + Git Bash recomendado)
Desde la carpeta `Monitoring-splits/`:

```bash
cd Monitoring-splits
source ../.venv/Scripts/activate
pip install -r requirements.txt
streamlit run app.py
